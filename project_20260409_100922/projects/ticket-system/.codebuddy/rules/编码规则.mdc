---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-04-14T02:18:40.978Z
provider: 
---

# 编码规则

**你是一个专业的软件工程师。请严格按照以下编码规范生成或修改代码。**

## 1. 代码风格
- 遵循语言官方推荐风格（如 Python 的 PEP8，JavaScript 的 StandardJS）
- 缩进使用 **4个空格**（禁止使用 Tab）
- 行宽不超过 **80-100** 字符
- 花括号换行风格：K&R 风格（左括号不换行）

## 2. 命名规则
- 变量/函数：`camelCase` 或 `snake_case`（根据语言惯例）
- 类/接口：`PascalCase`
- 常量：`UPPER_SNAKE_CASE`
- 私有属性/方法：以下划线开头 `_private`
- 命名必须具有实际语义，避免 `tmp`, `data` 等模糊命名
- **类名的结尾必须说明类的类型（如控制类以 `Controller` 结尾，服务类以 `Service` 结尾，实体类以 `Entity` 结尾，工具类以 `Util` 结尾）**

## 3. 注释与文档
- 公共 API（函数、类、模块）必须写文档注释
- 复杂逻辑前必须写说明注释
- 注释使用 **英文** 或 **中文**（统一）
- 禁止保留注释掉的代码

## 4. 错误处理
- 不要吞掉异常，至少要记录日志
- 不要使用 `except:` 裸异常捕获
- 外部输入（用户、文件、网络）必须做有效性校验

## 5. 可读性与维护性
- 单个函数不超过 **50 行**
- 单个文件不超过 **400 行**
- 嵌套层级不超过 **4 层**
- 避免重复代码（DRY 原则）
- 优先使用标准库和成熟依赖
- **所有通用的工具逻辑必须封装成独立工具类/工具函数，禁止在业务代码中重复实现相同功能**
- 代码量一定要是可实现的最小功能，不要过度封装，避免代码臃肿

## 6. 配置管理
- **通用的配置（如数据库连接、API密钥、环境参数、常量值）必须写在统一的配置类或配置文件中**
- 禁止将配置硬编码在业务逻辑代码中
- 配置类命名以 `Config` 结尾

## 7. 测试要求
- 关键逻辑需要提供简单测试用例
- 测试命名：`test_<功能>_<场景>`

## 8. 输出格式要求
- 直接输出代码，不要解释“这是你生成的代码”
- 如果需要修改原有代码，请用 diff 或完整文件形式给出
- 在代码块中标明语言类型（如 ```python）

---

**现在请根据以上规范，完成我接下来提出的编程任务。**