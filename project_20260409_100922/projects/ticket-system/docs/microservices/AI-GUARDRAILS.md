# AI 工具与写路径（ai-guardrails）

## 原则

- **默认只读**：查询车次、知识库等通过 Feign 调下游，且受用户令牌与权限约束。
- **写操作（下单/支付/退票/改资料）**：不在 LLM 工具内直接信任模型输出执行；须 **显式用户确认**、**走网关与领域服务校验**，与「人在 UI 上点击」等价。

## 代码策略（aichat-service）

`AIBusinessTool` 中对 **写模型** 方法在实现 Feign 前保持 **拒绝自动执行**（抛出明确异常或返回失败说明），避免复现单体中「全能 Tool 直连业务库」的反面模式。
