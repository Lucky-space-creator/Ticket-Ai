<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ticket.order.mapper.OrderItemMapper">
    <!-- 批量插入订单明细 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO order_item (id, order_id, passenger_name, id_card, price, created_at)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.id}, #{item.orderId}, #{item.passengerName}, #{item.idCard}, #{item.price}, #{item.createdAt})
        </foreach>
    </insert>
</mapper>
