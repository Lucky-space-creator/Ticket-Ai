# 12306购票系统+智能客服 前期需求文档

&gt; **版本**: v1.0  
&gt; **阶段**: 前期（单体架构）  
&gt; **日期**: 2026-04-08  
&gt; **技术栈**: Vue3 + TypeScript + Spring Boot 3.x + MySQL + Redis

---

## 1. 项目概述

### 1.1 项目背景
构建一个仿12306的铁路票务购票系统，集成AI智能客服，提供完整的用户购票体验和后台运营管理能力。

### 1.2 核心目标
- 提供稳定、高效的在线票务查询与购买服务
- 实现AI智能客服，解答常见问题，减轻人工客服压力
- 支持高并发场景下的车票查询与下单
- 提供完整的后台管理系统，支撑业务运营

### 1.3 用户角色
| 角色 | 描述 |
|------|------|
| 普通用户 | 注册/登录、查询车次、购票、退票、查看订单 |
| 管理员 | 车次管理、订单管理、用户管理、客服知识库管理 |
| 智能客服 | 自动回复用户咨询，处理常见问题 |

---

## 2. 功能需求

### 2.1 用户端功能模块

#### 2.1.1 用户认证模块
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 用户注册 | 手机号+设置密码 | P0 |
| 用户登录 | 账号密码登录 | P0 |
| 个人信息 | 查看/编辑个人资料、实名认证 | P0 |
| 常用联系人 | 添加/删除/编辑乘车人信息 | P0 |

#### 2.1.2 车票查询模块
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 车次查询 | 按出发地、目的地、日期查询车次 | P0 |
| 余票查询 | 实时显示各席别余票数量 | P0 |
| 车次筛选 | 按车型（高铁/动车/普快）、时间筛选 | P1 |
| 价格显示 | 显示各席别票价 | P0 |
| 中转方案 | 智能推荐中转方案 | P2 |

#### 2.1.3 购票下单模块
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 选座购票 | 选择车次、席别、乘车人 | P0 |
| 订单确认 | 确认订单信息、选择支付方式 | P0 |
| 订单支付 | 模拟支付流程（预留支付接口） | P0 |
| 候补购票 | 无票时加入候补队列 | P1 |
| 订单取消 | 未支付订单自动取消/手动取消 | P0 |

#### 2.1.4 订单管理模块
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 订单列表 | 查看全部/未出行/历史订单 | P0 |
| 订单详情 | 查看订单完整信息、乘车人、座位 | P0 |
| 退票申请 | 在线申请退票，原价退款 | P0 |
| 改签功能 | 支持改签其他车次（同线路） | P1 |
| 电子客票 | 生成电子客票二维码 | P1 |

#### 2.1.5 智能客服模块
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 在线对话 | 用户与AI客服实时对话 | P0 |
| 常见问题 | 预设常见问题快速回复 | P0 |
| 订单查询 | 通过客服查询个人订单状态 | P1 |
| 人工转接 | 复杂问题转人工客服 | P2 |
| 满意度评价 | 对客服服务进行评价 | P2 |

### 2.2 后台管理功能模块

#### 2.2.1 车次管理
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 车次录入 | 添加新车次信息（车次号、时刻表） | P0 |
| 车次编辑 | 修改车次信息、停靠站、时刻 | P0 |
| 座位管理 | 配置各车厢座位数、席别分布 | P0 |
| 余票调整 | 手动调整余票数量（应急） | P1 |
| 车次停运 | 设置车次停运/恢复运营 | P1 |

#### 2.2.2 订单管理
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 订单查询 | 按条件查询所有用户订单 | P0 |
| 订单详情 | 查看订单完整信息 | P0 |
| 异常处理 | 处理异常订单（退款、强制取消） | P1 |
| 订单统计 | 按日期/线路统计销售数据 | P1 |

#### 2.2.3 用户管理
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 用户列表 | 查看注册用户列表 | P0 |
| 用户详情 | 查看用户资料、订单历史 | P0 |
| 账号管控 | 禁用/启用用户账号 | P1 |
| 实名审核 | 审核用户实名认证信息 | P1 |

#### 2.2.4 客服知识库管理
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 知识录入 | 添加FAQ问答对 | P0 |
| 知识分类 | 按业务类型分类管理 | P0 |
| 知识训练 | 训练/更新AI客服模型 | P1 |
| 对话记录 | 查看用户与AI的对话历史 | P1 |
| 热点问题 | 统计分析高频问题 | P2 |

---

## 3. 非功能需求

### 3.1 性能需求
| 指标 | 目标值 | 说明 |
|------|--------|------|
| 页面加载 | &lt; 2s | 首屏加载时间 |
| 车次查询 | &lt; 500ms | 单次查询响应时间 |
| 并发支持 | 1000 QPS | 前期单体架构目标 |
| 下单接口 | &lt; 1s | 从提交到返回结果 |
| 数据库连接 | &lt; 100ms | 平均查询耗时 |

### 3.2 可用性需求
- **系统可用性**: 99.9%（允许年停机时间 &lt; 8.76小时）
- **数据备份**: 每日全量备份，Binlog增量备份
- **故障恢复**: 支持快速回滚，RTO &lt; 30分钟

### 3.3 安全需求
| 类别 | 要求 |
|------|------|
| 传输安全 | 全站HTTPS，TLS 1.2+ |
| 数据加密 | 敏感字段（密码、身份证）AES加密存储 |
| 防刷限流 | 接口防刷，验证码防机器注册 |
| SQL注入 | 使用MyBatis参数化查询，禁止拼接SQL |
| XSS防护 | 前端输入过滤，后端输出转义 |
| 权限控制 | RBAC权限模型，JWT Token认证 |

### 3.4 扩展性需求
- 预留微服务拆分接口，后期平滑迁移
- 数据库设计预留分库分表字段（如用户ID取模）
- 缓存设计预留分布式缓存扩展

---

## 4. 数据库设计（前期）

### 4.1 核心表结构

#### 用户相关表
```sql
-- 用户表
CREATE TABLE `t_user` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    `username` VARCHAR(50) NOT NULL COMMENT '用户名',
    `password` VARCHAR(100) NOT NULL COMMENT '加密密码',
    `phone` VARCHAR(20) NOT NULL COMMENT '手机号',
    `email` VARCHAR(100) COMMENT '邮箱',
    `real_name` VARCHAR(50) COMMENT '真实姓名',
    `id_card` VARCHAR(18) COMMENT '身份证号（加密）',
    `id_card_type` TINYINT DEFAULT 1 COMMENT '证件类型 1-身份证',
    `status` TINYINT DEFAULT 1 COMMENT '状态 0-禁用 1-正常',
    `is_real_name` TINYINT DEFAULT 0 COMMENT '是否实名 0-否 1-是',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_phone` (`phone`),
    UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB COMMENT='用户表';

-- 常用联系人表
CREATE TABLE `t_passenger` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `user_id` BIGINT NOT NULL COMMENT '所属用户ID',
    `name` VARCHAR(50) NOT NULL COMMENT '姓名',
    `id_card` VARCHAR(18) NOT NULL COMMENT '身份证号（加密）',
    `id_card_type` TINYINT DEFAULT 1 COMMENT '证件类型',
    `phone` VARCHAR(20) COMMENT '手机号',
    `passenger_type` TINYINT DEFAULT 1 COMMENT '乘客类型 1-成人 2-儿童 3-学生 4-军人',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB COMMENT='常用联系人/乘车人表';

-- 车次表
CREATE TABLE `t_train` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `train_no` VARCHAR(20) NOT NULL COMMENT '车次号 如G1234',
    `train_type` TINYINT NOT NULL COMMENT '车次类型 1-高铁 2-动车 3-普快',
    `start_station` VARCHAR(50) NOT NULL COMMENT '始发站',
    `end_station` VARCHAR(50) NOT NULL COMMENT '终到站',
    `start_time` TIME NOT NULL COMMENT '发车时间',
    `end_time` TIME NOT NULL COMMENT '到达时间',
    `run_days` INT DEFAULT 0 COMMENT '运行天数（跨天）',
    `status` TINYINT DEFAULT 1 COMMENT '状态 0-停运 1-正常',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_train_no` (`train_no`)
) ENGINE=InnoDB COMMENT='车次基础信息表';

-- 车次停靠站表
CREATE TABLE `t_train_station` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `train_id` BIGINT NOT NULL COMMENT '车次ID',
    `station_name` VARCHAR(50) NOT NULL COMMENT '站点名称',
    `station_no` INT NOT NULL COMMENT '站序',
    `arrive_time` TIME COMMENT '到达时间',
    `depart_time` TIME COMMENT '出发时间',
    `stop_minutes` INT DEFAULT 0 COMMENT '停车分钟数',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_train_id` (`train_id`)
) ENGINE=InnoDB COMMENT='车次停靠站时刻表';

-- 座位/余票表（按日期）
CREATE TABLE `t_ticket_stock` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `train_id` BIGINT NOT NULL COMMENT '车次ID',
    `train_date` DATE NOT NULL COMMENT '乘车日期',
    `start_station` VARCHAR(50) NOT NULL COMMENT '出发站',
    `end_station` VARCHAR(50) NOT NULL COMMENT '到达站',
    `seat_type` TINYINT NOT NULL COMMENT '席别 1-商务座 2-一等座 3-二等座 4-软卧 5-硬卧 6-硬座',
    `price` DECIMAL(10,2) NOT NULL COMMENT '票价',
    `total_seats` INT NOT NULL COMMENT '总座位数',
    `available_seats` INT NOT NULL COMMENT '剩余座位数',
    `version` INT DEFAULT 0 COMMENT '乐观锁版本号',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_train_date_seat` (`train_id`, `train_date`, `start_station`, `end_station`, `seat_type`),
    INDEX `idx_query` (`train_date`, `start_station`, `end_station`)
) ENGINE=InnoDB COMMENT='余票库存表';

-- 订单表
CREATE TABLE `t_order` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `order_no` VARCHAR(32) NOT NULL COMMENT '订单号',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `train_id` BIGINT NOT NULL COMMENT '车次ID',
    `train_no` VARCHAR(20) NOT NULL COMMENT '车次号',
    `train_date` DATE NOT NULL COMMENT '乘车日期',
    `start_station` VARCHAR(50) NOT NULL COMMENT '出发站',
    `end_station` VARCHAR(50) NOT NULL COMMENT '到达站',
    `depart_time` DATETIME NOT NULL COMMENT '发车时间',
    `arrive_time` DATETIME NOT NULL COMMENT '到达时间',
    `seat_type` TINYINT NOT NULL COMMENT '席别',
    `total_amount` DECIMAL(10,2) NOT NULL COMMENT '订单总金额',
    `status` TINYINT NOT NULL COMMENT '状态 0-待支付 1-已支付 2-已出票 3-已退票 4-已取消',
    `pay_time` DATETIME COMMENT '支付时间',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_order_no` (`order_no`),
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_status_created` (`status`, `created_at`)
) ENGINE=InnoDB COMMENT='订单主表';

-- 订单乘客明细表
CREATE TABLE `t_order_item` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `order_id` BIGINT NOT NULL COMMENT '订单ID',
    `passenger_id` BIGINT NOT NULL COMMENT '乘客ID',
    `passenger_name` VARCHAR(50) NOT NULL COMMENT '乘客姓名',
    `id_card` VARCHAR(18) NOT NULL COMMENT '身份证号（加密）',
    `id_card_type` TINYINT DEFAULT 1 COMMENT '证件类型',
    `seat_no` VARCHAR(10) COMMENT '座位号 如 05车12A',
    `ticket_no` VARCHAR(20) COMMENT '电子客票号',
    `ticket_status` TINYINT DEFAULT 1 COMMENT '票状态 1-正常 2-已退',
    `price` DECIMAL(10,2) NOT NULL COMMENT '单张票价',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_order_id` (`order_id`)
) ENGINE=InnoDB COMMENT='订单乘客明细表';

-- 客服知识库表
CREATE TABLE `t_knowledge_base` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `category_id` INT NOT NULL COMMENT '分类ID',
    `question` VARCHAR(500) NOT NULL COMMENT '问题',
    `answer` TEXT NOT NULL COMMENT '答案',
    `keywords` VARCHAR(200) COMMENT '关键词，逗号分隔',
    `hit_count` INT DEFAULT 0 COMMENT '命中次数',
    `status` TINYINT DEFAULT 1 COMMENT '状态 0-禁用 1-启用',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_category` (`category_id`),
    FULLTEXT INDEX `ft_question` (`question`)
) ENGINE=InnoDB COMMENT='客服知识库';

-- 客服对话记录表
CREATE TABLE `t_chat_record` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `user_id` BIGINT COMMENT '用户ID（未登录为NULL）',
    `session_id` VARCHAR(64) NOT NULL COMMENT '会话ID',
    `message` TEXT NOT NULL COMMENT '消息内容',
    `msg_type` TINYINT NOT NULL COMMENT '类型 1-用户 2-机器人 3-人工',
    `intent` VARCHAR(50) COMMENT '识别意图',
    `confidence` DECIMAL(3,2) COMMENT '置信度',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_session` (`session_id`, `created_at`),
    INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB COMMENT='客服对话记录';