# 12306购票系统+智能客服 简化版需求文档

> **版本**: v2.0-simplified  
> **阶段**: MVP版本  
> **日期**: 2026-04-08  
> **技术栈**: Vue3 + Spring Boot + MySQL + Redis

---

## 1. 项目概述

### 1.1 核心目标
构建精简版铁路票务系统，保留核心购票流程，集成AI智能客服解答常见问题。

### 1.2 用户角色
| 角色 | 核心功能 |
|------|----------|
| 普通用户 | 注册登录、查票购票、查看订单、咨询客服 |
| 管理员 | 车次管理、订单查看、客服知识库维护 |

---

## 2. 功能需求（精简版）

### 2.1 用户端核心功能

| 模块 | 保留功能 | 移除功能 |
|------|----------|----------|
| **用户认证** | 手机号注册/登录、实名认证 | 邮箱登录、第三方登录 |
| **车票查询** | 按起终点+日期查车次、显示余票价格 | 中转方案、多维度筛选 |
| **购票下单** | 选座购票、订单确认、模拟支付 | 候补购票、改签、选座偏好 |
| **订单管理** | 查看订单列表、退票申请 | 改签、电子客票二维码 |
| **智能客服** | **在线对话、FAQ问答、订单状态查询** | 人工转接、满意度评价 |

### 2.2 管理端核心功能

| 模块 | 保留功能 |
|------|----------|
| **车次管理** | 添加/编辑车次、配置座位数、设置停运 |
| **订单管理** | 查看订单列表、订单详情 |
| **用户管理** | 查看用户列表、禁用账号 |
| **客服知识库** | FAQ增删改查、查看对话记录 |

---

## 3. 非功能需求（精简）

| 指标 | 目标值 |
|------|--------|
| 页面加载 | < 3s |
| 车次查询 | < 1s |
| 并发支持 | 800 QPS |
| 系统可用性 | 99% |

**安全措施**: HTTPS传输、密码加密、SQL注入防护、JWT认证

---

## 4. 数据库设计（精简版）

### 4.1 表结构清单

共 **8张核心表**：用户、乘客、车次、停靠站、余票、订单、订单明细、知识库

---

### 4.2 建表SQL

```sql
-- ==================== 用户模块 ====================

-- 用户表（合并实名信息，简化字段）
CREATE TABLE `user` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    `phone` VARCHAR(20) NOT NULL COMMENT '手机号（登录账号）',
    `password` VARCHAR(100) NOT NULL COMMENT '加密密码',
    `real_name` VARCHAR(50) COMMENT '真实姓名',
    `id_card` VARCHAR(18) COMMENT '身份证号（加密存储）',
    `status` TINYINT DEFAULT 1 COMMENT '状态 0-禁用 1-正常',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_phone` (`phone`)
) ENGINE=InnoDB COMMENT='用户表';

-- 常用联系人表（简化乘客类型）
CREATE TABLE `passenger` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `user_id` BIGINT NOT NULL COMMENT '所属用户ID',
    `name` VARCHAR(50) NOT NULL COMMENT '姓名',
    `id_card` VARCHAR(18) NOT NULL COMMENT '身份证号（加密）',
    `phone` VARCHAR(20) COMMENT '手机号',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB COMMENT='常用联系人表';

-- ==================== 车次模块 ====================

-- 车次表（简化：移除运行天数等次要字段）
CREATE TABLE `train` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `train_no` VARCHAR(20) NOT NULL COMMENT '车次号 如G1234',
    `train_type` TINYINT NOT NULL COMMENT '类型 1-高铁 2-动车 3-普快',
    `start_station` VARCHAR(50) NOT NULL COMMENT '始发站',
    `end_station` VARCHAR(50) NOT NULL COMMENT '终到站',
    `start_time` TIME NOT NULL COMMENT '发车时间',
    `end_time` TIME NOT NULL COMMENT '到达时间',
    `status` TINYINT DEFAULT 1 COMMENT '状态 0-停运 1-正常',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_train_no` (`train_no`)
) ENGINE=InnoDB COMMENT='车次基础信息表';

-- 车次停靠站表（保留核心字段）
CREATE TABLE `train_station` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `train_id` BIGINT NOT NULL COMMENT '车次ID',
    `station_name` VARCHAR(50) NOT NULL COMMENT '站点名称',
    `station_no` INT NOT NULL COMMENT '站序(第几站)',
    `arrive_time` TIME COMMENT '到达时间',
    `depart_time` TIME COMMENT '出发时间',
    INDEX `idx_train_id` (`train_id`)
) ENGINE=InnoDB COMMENT='车次停靠站时刻表';

-- 余票库存表（简化：移除乐观锁版本号，使用数据库锁）
CREATE TABLE `ticket_stock` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `train_id` BIGINT NOT NULL COMMENT '车次ID',
    `train_date` DATE NOT NULL COMMENT '乘车日期',
    `start_station` VARCHAR(50) NOT NULL COMMENT '出发站',
    `end_station` VARCHAR(50) NOT NULL COMMENT '到达站',
    `seat_type` TINYINT NOT NULL COMMENT '席别 1-商务 2-一等 3-二等 4-软卧 5-硬卧 6-硬座',
    `price` DECIMAL(10,2) NOT NULL COMMENT '票价',
    `available_seats` INT NOT NULL DEFAULT 0 COMMENT '剩余座位数',
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_stock` (`train_id`, `train_date`, `start_station`, `end_station`, `seat_type`),
    INDEX `idx_query` (`train_date`, `start_station`, `end_station`)
) ENGINE=InnoDB COMMENT='余票库存表';

-- ==================== 订单模块 ====================

-- 订单主表（合并状态，简化支付相关字段）
CREATE TABLE `order` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `order_no` VARCHAR(32) NOT NULL COMMENT '订单号',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `train_id` BIGINT NOT NULL COMMENT '车次ID',
    `train_no` VARCHAR(20) NOT NULL COMMENT '车次号',
    `train_date` DATE NOT NULL COMMENT '乘车日期',
    `start_station` VARCHAR(50) NOT NULL COMMENT '出发站',
    `end_station` VARCHAR(50) NOT NULL COMMENT '到达站',
    `depart_time` DATETIME NOT NULL COMMENT '发车时间',
    `seat_type` TINYINT NOT NULL COMMENT '席别',
    `total_amount` DECIMAL(10,2) NOT NULL COMMENT '订单总金额',
    `status` TINYINT NOT NULL DEFAULT 0 COMMENT '状态 0-待支付 1-已支付 2-已退票 3-已取消',
    `pay_time` DATETIME COMMENT '支付时间',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_order_no` (`order_no`),
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_status` (`status`)
) ENGINE=InnoDB COMMENT='订单主表';

-- 订单乘客明细表（简化：移除座位号、电子客票号）
CREATE TABLE `order_item` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `order_id` BIGINT NOT NULL COMMENT '订单ID',
    `passenger_name` VARCHAR(50) NOT NULL COMMENT '乘客姓名',
    `id_card` VARCHAR(18) NOT NULL COMMENT '身份证号（加密）',
    `price` DECIMAL(10,2) NOT NULL COMMENT '单张票价',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_order_id` (`order_id`)
) ENGINE=InnoDB COMMENT='订单乘客明细表';

-- ==================== AI客服模块（核心保留）====================

-- 客服知识库表（保留全文检索，简化分类）
CREATE TABLE `knowledge_base` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `category` VARCHAR(50) DEFAULT 'general' COMMENT '分类 如booking/refund/query',
    `question` VARCHAR(500) NOT NULL COMMENT '问题',
    `answer` TEXT NOT NULL COMMENT '答案',
    `keywords` VARCHAR(200) COMMENT '关键词，逗号分隔',
    `hit_count` INT DEFAULT 0 COMMENT '命中次数（用于优化）',
    `status` TINYINT DEFAULT 1 COMMENT '状态 0-禁用 1-启用',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FULLTEXT INDEX `ft_question` (`question`)
) ENGINE=InnoDB COMMENT='AI客服知识库';

-- 客服对话记录表（简化：移除置信度等AI细节字段）
CREATE TABLE `chat_record` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `user_id` BIGINT COMMENT '用户ID（未登录为NULL）',
    `session_id` VARCHAR(64) NOT NULL COMMENT '会话ID',
    `message` TEXT NOT NULL COMMENT '消息内容',
    `msg_type` TINYINT NOT NULL COMMENT '类型 1-用户 2-机器人',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `confidence` DECIMAL(3,2) COMMENT '置信度',
    INDEX `idx_session` (`session_id`, `created_at`),
    INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB COMMENT='客服对话记录';
```

---

## 5. 功能裁剪说明

### 5.1 已移除的非核心功能

| 原功能 | 移除原因 | 替代方案 |
|--------|----------|----------|
| 候补购票 | 复杂度高，需队列系统 | 提示用户无票，建议换日期 |
| 改签功能 | 涉及库存回滚再锁定 | 引导用户退票后重新购买 |
| 电子客票二维码 | 需生成图片/验票逻辑 | 仅显示订单号+身份信息 |
| 人工客服转接 | 需实时通讯系统 | AI客服覆盖80%常见问题 |
| 满意度评价 | 非核心功能 | 暂不实现 |
| 订单统计报表 | 管理端非急需 | 直接SQL查询 |
| 实名审核流程 | 简化流程 | 用户自主填写，后台抽查 |

### 5.2 保留的AI客服能力

```
用户咨询 → 关键词匹配/全文检索 → 返回知识库答案
                    ↓
              订单相关查询 → 调用订单接口查询状态
                    ↓
              无匹配问题 → 返回兜底回复（引导转人工电话）
```

**知识库分类设计**:
- `booking`: 购票相关问题（如何购票、支付方式）
- `refund`: 退票相关问题（退票规则、退款时间）
- `query`: 查询相关问题（查订单、查余票）
- `common`: 常见问题（携带物品、进站流程）

---

## 6. 接口清单（精简）

### 6.1 用户端接口（15个）

| 模块 | 接口 | 说明 |
|------|------|------|
| 认证 | POST /api/auth/register | 手机号注册 |
| 认证 | POST /api/auth/login | 登录 |
| 用户 | GET /api/user/profile | 获取个人信息 |
| 用户 | PUT /api/user/profile | 更新个人信息 |
| 乘客 | GET /api/passengers | 获取常用联系人 |
| 乘客 | POST /api/passengers | 添加联系人 |
| 乘客 | DELETE /api/passengers/{id} | 删除联系人 |
| 车次 | GET /api/trains/search | 查询车次余票 |
| 订单 | POST /api/orders | 创建订单 |
| 订单 | POST /api/orders/{id}/pay | 模拟支付 |
| 订单 | GET /api/orders | 获取订单列表 |
| 订单 | GET /api/orders/{id} | 获取订单详情 |
| 订单 | POST /api/orders/{id}/refund | 申请退票 |
| **客服** | **POST /api/chat/send** | **发送消息** |
| **客服** | **GET /api/chat/history** | **获取对话历史** |

### 6.2 管理端接口（10个）

| 模块 | 接口 | 说明 |
|------|------|------|
| 车次 | POST /api/admin/trains | 添加车次 |
| 车次 | PUT /api/admin/trains/{id} | 编辑车次 |
| 车次 | POST /api/admin/trains/{id}/stock | 设置余票 |
| 订单 | GET /api/admin/orders | 查看所有订单 |
| 用户 | GET /api/admin/users | 查看用户列表 |
| 用户 | PUT /api/admin/users/{id}/status | 禁用/启用用户 |
| **知识库** | **GET /api/admin/knowledge** | **获取FAQ列表** |
| **知识库** | **POST /api/admin/knowledge** | **添加FAQ** |
| **知识库** | **PUT /api/admin/knowledge/{id}** | **更新FAQ** |
| **知识库** | **DELETE /api/admin/knowledge/{id}** | **删除FAQ** |

---

## 7. 开发优先级

### Phase 1（核心流程）
1. 用户注册/登录
2. 车次查询（静态数据）
3. 下单+支付流程
4. 订单查看

### Phase 2（AI客服）
5. 知识库管理后台
6. AI对话接口（关键词匹配版）
7. 客服查询订单状态

### Phase 3（完善）
8. 退票功能
9. 余票库存管理
10. 管理端其他功能

---
